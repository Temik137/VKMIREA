<UserControl x:Class="VkMirea.CustomControls.Block"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VkMirea.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="200"
             DataContext="{Binding BlockVM, Source={StaticResource Locator}}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Image Grid.Row="0" Source="{Binding CurrentImage}" Width="{Binding Path=ActualHeight, RelativeSource={RelativeSource Self}}"/>
        <Label Grid.Row="1" Content="{Binding Text}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
    </Grid>
</UserControl>
