<UserControl x:Class="VkMirea.CustomControls.Block"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VkMirea.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="200"
             x:Name="BlockElement">
    <Border BorderThickness="1" BorderBrush="DarkGray">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="3*"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Image Grid.Row="0" Source="{Binding Path=ImageSource, ElementName=BlockElement}" MaxHeight="70" MaxWidth="70" Width="{Binding Path=ActualHeight, ElementName=BlockElement}"/>
            <Label Grid.Row="1" Content="{Binding Path=TitleName, ElementName=BlockElement}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Grid>
    </Border>
</UserControl>
